{
  "name": "@irc/admin",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "IRC Emergency Platform — Administration",
  "dependencies": {
    "@irc/shared": "*"
  },
  "scripts": {
    "build": "node build.js",
    "prebuild": "npm -w @irc/shared run build"
  }
}
